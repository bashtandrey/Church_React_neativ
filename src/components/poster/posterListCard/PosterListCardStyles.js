import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  listContainer: {
    paddingVertical: 10,
  },
  emptyText: {
  textAlign: "center",
  marginTop: 40,
  fontSize: 16,
  color: "#6c757d", // серый текст, как в Bootstrap
  fontStyle: "italic",
}
});

export default styles;