import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
  },
  authButton: {
    marginHorizontal: 8,
    padding: 6,
  },
});

export default styles;